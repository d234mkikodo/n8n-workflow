# 🤖 n8n Job TTS Automation Workflow (ElevenLabs & Discord)

## 📌 ภาพรวมโครงการ

โปรเจกต์นี้คือ n8n workflow ที่สร้างขึ้นเพื่อ **แปลงข้อมูลรายละเอียดงาน/อาชีพ (Job Description) ให้เป็นเสียงพูด (Text-to-Speech - TTS)** โดยใช้บริการ **ElevenLabs** และส่งไฟล์เสียงพร้อมข้อมูลสรุปไปยังช่องทาง **Discord** โดยอัตโนมัติ

**Workflow นี้มีประโยชน์สำหรับ:**

* การสร้างเนื้อหาเสียงสำหรับการศึกษาหรือการนำเสนออาชีพ
* การเผยแพร่ข้อมูลงานใหม่ๆ ในรูปแบบเสียงไปยังแพลตฟอร์ม Discord
* การทดลองใช้งานระบบอัตโนมัติสำหรับสื่อมัลติมีเดีย

## 🛠️ โครงสร้าง Workflow

Workflow นี้ประกอบด้วย 4 ส่วนหลัก:

1.  **Job Data Fetch/Input:** รับข้อมูลดิบของรายละเอียดงาน (เช่น ชื่อตำแหน่ง, หน้าที่, คุณสมบัติ)
2.  **Script Generation/Processing:** ประมวลผลและจัดรูปแบบข้อมูลให้เป็นสคริปต์ที่พร้อมสำหรับการแปลงเสียง (อาจใช้ Node JavaScript หรือ AI/LLM Node)
3.  **ElevenLabs TTS Conversion:** แปลงสคริปต์ที่ได้ให้เป็นไฟล์เสียง (.mp3 หรือ .wav)
4.  **Discord Notification & File Upload:** อัปโหลดไฟล์เสียงที่สร้างเสร็จแล้ว พร้อมส่งข้อความแจ้งเตือนไปยังช่อง Discord ที่กำหนด

## ⚙️ ข้อกำหนดเบื้องต้น (Prerequisites)

เพื่อให้ Workflow นี้ทำงานได้อย่างถูกต้อง คุณต้องมีสิ่งเหล่านี้:

* **n8n Instance:** ติดตั้งและใช้งาน n8n Server (Self-hosted หรือ Cloud)
* **ElevenLabs API Key:** คีย์ API สำหรับการเข้าถึงบริการ ElevenLabs
* **Discord Webhook URL:** Webhook URL สำหรับช่อง Discord ที่คุณต้องการให้ไฟล์เสียงถูกส่งไป
* **Source Data:** ต้องมีแหล่งข้อมูลที่ป้อนรายละเอียดงานเข้าสู่ Workflow (เช่น ไฟล์ CSV, Google Sheets, หรือ API)

## 🔑 การตั้งค่า Credentials

คุณจำเป็นต้องตั้งค่า Credential ใน n8n สำหรับการเชื่อมต่อภายนอก:

| บริการ | Credential Type | ข้อมูลที่จำเป็น |
| :--- | :--- | :--- |
| **ElevenLabs** | API Key (HTTP Header/Basic Auth) | `X-API-Key` หรือใช้ n8n ElevenLabs Node |
| **Discord** | Webhook URL | `https://discord.com/api/webhooks/...` |
| **Source Data** | (ขึ้นอยู่กับแหล่งข้อมูล) | เช่น Google Sheet API Key หรือ Database Credentials |

## 🚀 วิธีการใช้งาน (How to Use)

1.  **นำเข้า Workflow:** คัดลอกเนื้อหาทั้งหมดในไฟล์ `job-tts-workflow.json` นี้ ไปวาง (Import) ใน n8n Editor
2.  **เชื่อมต่อ Credentials:** อัปเดต Node ของ ElevenLabs และ Discord ให้เชื่อมต่อกับ Credentials ที่คุณได้สร้างไว้
3.  **ตั้งค่า Input:** ปรับแต่ง Node เริ่มต้น (Trigger Node หรือ Input Node) ให้ดึงข้อมูลรายละเอียดงานจากแหล่งที่คุณต้องการ
4.  **ทดสอบ:** รัน Workflow ด้วยตนเองเพื่อตรวจสอบว่าไฟล์เสียงถูกสร้างและส่งไปยัง Discord สำเร็จหรือไม่
5.  **เปิดใช้งาน:** ตั้งค่า Workflow ให้ทำงานอัตโนมัติ (เช่น ทุกวัน หรือตามการเปลี่ยนแปลงของข้อมูล)

## 🤝 การสนับสนุน (Support)

หากมีคำถามหรือต้องการความช่วยเหลือในการปรับแต่ง Workflow นี้ กรุณาติดต่อ [ระบุช่องทางติดต่อของคุณ เช่น อีเมล หรือ Discord ID]
